import React from "react"
import { SafeAreaView, ScrollView, Text } from "react-native"

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>TODO</Text>
      </ScrollView>
    </SafeAreaView>
  )
}

export default App
